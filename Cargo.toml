[package]
name = "toxicblend"
version = "0.0.17"
authors = ["eadf"]
edition = "2018"
description = "gRPC based Blender-addons. WIP"
readme = "README.md"
repository = "https://github.com/eadf/toxicblend.rs"
license = "AGPL-3.0"
keywords = ["blender-addon", "centerline", "voronoi", "Lindenmayer_3d"]
exclude = [
    "img/*",
    "idea/*",
    ".github/*",
]

[features]
# This is for debugging purposes, will place small blobs at each corner of each bb voxel chunk
display_chunks = []
# enable the saft mesher backend
saft = ["saft_cr", "glam_saft"]

[dependencies]
tonic = "0.5"
prost = "0.8"
ahash = "0.7"
tokio = { version = "1.11", features = ["macros", "rt-multi-thread"] }
thiserror = "1.0"
linestring = "0.7"
centerline = "0.6"
vob = "3.0"
boostvoronoi = "0.9.2"
rayon = "1.5"
smallvec = "^1.6.1"
cgmath = "0.18"
itertools = "0.10"
logos = "0.12"
arrayvec = "0.7"
building-blocks = { git="https://github.com/bonsairobo/building-blocks.git", rev="21165e0", default-features = false, features = ["mesh", "cgmath"]}
#building-blocks = { path="../building-blocks", default-features = false, features = ["mesh", "cgmath"]}
ilattice = { git="https://github.com/bonsairobo/ilattice-rs.git", rev="ab57886", default-features = false, features = ["glam", "morton-encoding"]}
#ilattice = { path="../ilattice-rs", default-features = false, features = ["glam", "morton-encoding"]}
fast-surface-nets = "0.1.0"
saft_cr = { package="saft", version="0.20", optional = true, features = ["rayon", "with_rayon"]}
# The glam version used by saft/macaw
glam_saft = { package="glam", version="0.17", optional = true}
glam = "0.19"
bytemuck = "1.7"

[[bin]]
name = "toxicblend_server"
path = "src/server.rs"

[build-dependencies]
tonic-build = "0.5"
version_check = "0.9"

[profile.release]
lto = true

[badges]
maintenance = { status = "actively-developed" }
